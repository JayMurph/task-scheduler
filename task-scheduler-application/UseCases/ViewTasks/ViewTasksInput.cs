namespace task_scheduler_application.UseCases.ViewTasks {
    /// <summary>
    /// Encapsulates the input required for the <see cref="ViewTasksUseCase"/>
    /// </summary>
    public class ViewTasksInput {
        /*
         * Despite being empty, this exists so that the ViewTasks Use-Case can implement the
         * interface of IUseCase, which requires an input type.
         */
        /*
         * Properties could be added in for later expansion of the ViewTasks Use-Case
         */
    }
}
